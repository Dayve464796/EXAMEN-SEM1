@model aspnet_client.Models.Burger

<div class="container mt-5">
    <h2>🛒 Commander : @Model.Nom</h2>

    <form method="post">
        <input type="hidden" name="BurgerId" value="@Model.Id" />

        <h5 class="mt-4">Choisir des compléments :</h5>

        @foreach (var c in ViewBag.Complements)
        {
            <div class="form-check">
                <input class="form-check-input" type="checkbox" name="Complements" value="@c.Id" />
                <label class="form-check-label">
                    @c.Nom (@c.Prix FCFA)
                </label>
            </div>
        }

        <h5 class="mt-4">Type de commande :</h5>
        <select class="form-control" name="TypeCommande">
            <option value="Sur place">Sur place</option>
            <option value="À emporter">À emporter</option>
            <option value="Livraison">Livraison</option>
        </select>

        <button class="btn btn-brasil mt-4">Valider la commande</button>
    </form>
</div>